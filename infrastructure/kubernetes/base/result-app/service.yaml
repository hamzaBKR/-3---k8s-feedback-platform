apiVersion: v1
kind: Service

metadata:
  name: result-app
  namespace: feedback-app

spec:
  type: NodePort

  selector:
    app: result-app

  ports:
  - port: 4000
    targetPort: 4000
    nodePort: 30081
