apiVersion: v1
kind: Service

metadata:
  name: vote-app
  namespace: feedback-app

spec:
  selector:
    app: vote-app

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080

  type: NodePort
